# first-pr-practice

A tiny practice repo for shipping a first pull request.

## Contents

- `greet.py` — a couple of small helper functions (`greet`, `add`).
- `tests/` — unit tests for `greet.py`.

## Running tests

```
python -m unittest discover -s tests
```
